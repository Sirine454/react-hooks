
import React from 'react';

const Addmovie = ({onChange, placeholder}) => {
    return (


<div className="Add">
     <span className="AddSpan">
        
      </span>
        <input
          className="AddInput"
          type="add a movie"
          onChange={onChange}
          placeholder={placeholder}
        />
        
      </div>
    )}
      export default Addmovie